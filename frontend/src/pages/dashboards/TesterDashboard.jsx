import { useEffect,useState } from 'react';import { Navigate,Route,Routes,useLocation,useNavigate } from 'react-router-dom';import { ClipboardCheck,Activity,Bug } from 'lucide-react';import DashboardLayout from '../../layouts/DashboardLayout.jsx';import GlassCard from '../../components/GlassCard.jsx';import StatCard from '../../components/StatCard.jsx';import ProfilePanel from '../../components/dashboard/ProfilePanel.jsx';import TesterCases from '../../components/tester/TesterCases.jsx';import TesterWorkspace from '../../components/tester/TesterWorkspace.jsx';import TesterLists from '../../components/tester/TesterLists.jsx';import { getRoleNavigation } from '../../config/roleNavigation.js';import { dashboardService } from '../../services/dashboardService.js';import NotificationPage from '../../components/dashboard/NotificationPage.jsx'
const paths={overview:'/tester/dashboard',testCases:'/tester/test-cases',executions:'/tester/executions',bugs:'/tester/bugs',notifications:'/tester/notifications',profile:'/tester/profile'}
export default function TesterDashboard(){const nav=useNavigate(),loc=useLocation(),items=getRoleNavigation('TESTER');const active=loc.pathname.startsWith('/tester/test-cases/')?'testCases':Object.entries(paths).find(([,p])=>p===loc.pathname)?.[0]||'overview';return <DashboardLayout items={items} active={active} onSelect={k=>nav(paths[k])}><Routes><Route path="dashboard" element={<Overview/>}/><Route path="test-cases" element={<TesterCases/>}/><Route path="test-cases/:id" element={<TesterWorkspace/>}/><Route path="executions" element={<TesterLists mode="executions"/>}/><Route path="bugs" element={<TesterLists mode="bugs"/>}/><Route path="notifications" element={<NotificationPage/>}/><Route path="profile" element={<><TesterHeader title="Profile" subtitle="Your authenticated Tester account."/><ProfilePanel/></>}/><Route path="*" element={<Navigate to="dashboard" replace/>}/></Routes></DashboardLayout>}
function Overview(){const[s,setS]=useState(null),[e,setE]=useState('');useEffect(()=>{dashboardService.getSummary().then(r=>setS(r.data)).catch(x=>setE(x.message))},[]);const icons=[ClipboardCheck,Activity,Activity,Activity,Bug];return <><TesterHeader title="Tester Dashboard" subtitle="Assigned tests, current results, and reported defects."/>{e&&<GlassCard className="p-6 text-red-300">{e}</GlassCard>}{!s&&!e&&<GlassCard className="p-6 text-slate-400">Loading dashboard…</GlassCard>}{s&&<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{s.metrics.map((m,i)=><StatCard key={m.key} icon={icons[i%icons.length]} label={m.label} value={m.value} delta={m.value===0?m.emptyMessage:undefined} index={i}/>)}</div>}</>}
export function TesterHeader({title,subtitle,action}){return <div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><span className="text-xs font-semibold uppercase tracking-widest text-amber-300">Tester</span><h1 className="text-2xl font-bold text-white sm:text-3xl">{title}</h1>{subtitle&&<p className="mt-1 text-sm text-slate-400">{subtitle}</p>}</div>{action}</div>}
function Placeholder(){return <><TesterHeader title="Notifications"/><GlassCard hover={false} className="p-6 text-slate-400">Notifications will appear here when the notification backend is enabled.</GlassCard></>}
