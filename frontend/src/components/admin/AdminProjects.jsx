import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import GlassCard from '../GlassCard.jsx'
import { projectService } from '../../services/projectService.js'
import { date, Empty, Error, Status } from '../client/ClientProjects.jsx'
import { AdminHeader } from '../../pages/dashboards/AdminDashboard.jsx'
export default function AdminProjects() { const [projects,setProjects]=useState([]); const [error,setError]=useState(''); useEffect(()=>{projectService.getProjects({size:100,sort:'createdAt,desc'}).then((r)=>setProjects(r.data.content||[])).catch((e)=>setError(e.message))},[]); return <><AdminHeader title="Projects" subtitle="System-wide projects and project-member access." />{error&&<Error message={error}/>} {!error&&projects.length===0&&<Empty message="No projects found."/>}{projects.length>0&&<GlassCard hover={false} className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase text-slate-400"><tr>{['Project','Client','Status','Requirements','Created','Action'].map((h)=><th key={h} className="px-5 py-4">{h}</th>)}</tr></thead><tbody>{projects.map((p)=><tr key={p.id} className="border-b border-white/[.07]"><td className="px-5 py-4 font-medium text-white">{p.name}</td><td className="px-5 py-4 text-slate-300">{p.client?.name}</td><td className="px-5 py-4"><Status value={p.status}/></td><td className="px-5 py-4 text-slate-300">{p.requirementCount}</td><td className="px-5 py-4 text-slate-400">{date(p.createdAt)}</td><td className="px-5 py-4"><Link className="whitespace-nowrap text-blue-300 hover:text-blue-200" to={`/admin/projects/${p.id}`}>Project Members</Link></td></tr>)}</tbody></table></GlassCard>}</> }
