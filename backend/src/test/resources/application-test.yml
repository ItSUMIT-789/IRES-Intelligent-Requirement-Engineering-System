spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ires_test
    username: ires
    password: change-me
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true

  test:
    database:
      replace: none
